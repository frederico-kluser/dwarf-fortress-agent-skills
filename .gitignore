# Python venv
.venv/
__pycache__/
*.pyc

# Conteúdo bruto re-coletável da API (regenerável via .work/crawl_api.py).
# Grande demais para versionar; o crawl reconstrói a partir da wiki viva.
.work/*.jsonl
.work/*.xml
.work/*.xml.zst
.work/crawl.log

# Índice de busca FTS5 (artefato derivado; search.py reconstrói no 1º uso).
.agents/skills/scripts/index.db

# Versionado:
#   .agents/skills/**                      (produto: skills + artigos markdown)
#   .agents/skills/scripts/*.py *.tsv      (busca + glossário)
#   .work/crawl_api.py build_skills.py htmlmd.py taxonomy.py   (pipeline)
#   requirements.txt

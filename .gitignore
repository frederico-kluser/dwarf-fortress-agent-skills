# Python venv
.venv/
__pycache__/
*.pyc

# Raw wiki dump + intermediates (regenerable via .work/parse_dump.py)
.work/*.xml
.work/*.xml.zst
.work/*.jsonl

# Analysis intermediates (regenerable)
.work/page_cats.json
.work/cat_to_pages.json
.work/categories.json
.work/cat_counts.json

# Kept versioned:
#   .agents/skills/**          (product: flat skill tree + articles)
#   .work/*.py                 (generation pipeline)
#   .work/build_summary.json
